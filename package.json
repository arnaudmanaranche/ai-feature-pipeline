{
  "name": "ai-feature-pipeline-dev",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling for the AI Feature Pipeline module (not published, not installed by consumers).",
  "scripts": {
    "test": "node --import tsx --test skills/afp-pipeline/scripts/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
